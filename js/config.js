const CONFIG = {
    // Set to your Fly.io API URL in production, e.g. 'https://projectcanvas-api.fly.dev'
    // Leave empty to fall back to local data.json (for development/design mode)
    API_BASE_URL: 'https://projectcanvas-api.fly.dev',
};
